@layout('master')
@section('content')
   <h1> {{ title }} </h1>
  
   @if(flashMessage('notification'))
       <div class="notification is-danger">
           {{ flashMessage('notification') }}
       </div>
   @endif

   <form method="POST" action="/forgot-password">
    {{ csrfField() }}
    <div class="field">
      <label class="label" for="exampleInputEmail1">Email address</label>
      <div class="control">
        <input name="email" class="input" type="email" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter email">
      </div>
    </div>
    
    <div class="control">
      <button name="login" type="submit" class="button is-primary">Reset Password</button>
    </div>
  </form>

 @endsection
