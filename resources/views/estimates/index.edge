@layout('master')

@section('content')
List  estimates
<div class="box">
    <div class="float-right"><a href="{{ route('/estimates/create') }}">
            Create Estimate
          </a></div>
    <h1 class="title">estimate List</h1>

    @if(old('notification'))
        <div class="notification is-success">
            {{ old('notification') }}
        </div>
    @endif
    @if(estimates.length)  
    <table class="table is-bordered is-striped is-narrow is-fullwidth">
        <thead>
          <tr>

            <th>SN</th>
            <th>Name</th>
            
            <th colspan="3" class="text-center">Actions</th> 
          </tr>
        </thead>
        <tbody>
            @each(estimate in estimates)
            <tr>
                <td>
                  
                    {{ ($loop.index + 1) }}
                </td>
                <td>
                    {{ estimate.job_name }}
                </td>
                <td class="text-center">
                    <a href="{{ '/estimates/' + estimate.id }}" class="btn btn-primary">View</a>
                </td>
                <td class="text-center">
                   
                    <a href="{{ '/estimates/' + estimate.id + '/edit' }}" class="btn btn-primary">Edit</a>
                </td>
                <td class="text-center">
                    <form action="{{ 'estimates/' + estimate.id + '?_method=DELETE' }}" method="POST">
                        {{ csrfField() }}
                        <button class="button is-danger" type="submit">Delete</button>
                    </form>                    
                </td>
            </tr>
            @endeach
        </tbody>
      </table>
      <table>
          <p>new estimates material</p>
      </table>


      
      @else
        <div class="alert alert-warning">No estimates created yet!</div>
      @endif
               
</div>
@endsection